import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/**
 * Pi Agent View extension entry point.
 *
 * Functionality will be added through the approved product spec and tickets.
 */
export default function agentView(_pi: ExtensionAPI) {}
